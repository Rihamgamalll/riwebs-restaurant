PORT=8080
NODE_ENV=production
FRONTEND_URL=https://your-vercel-domain.vercel.app
# Optional Railway Postgres plugin connection string. If omitted, checkout still works but orders are not persisted.
DATABASE_URL=
