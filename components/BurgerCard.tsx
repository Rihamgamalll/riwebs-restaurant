'use client';
import Image from 'next/image'; import {Plus} from 'lucide-react'; import type {MenuItem} from '@/data/menu'; import {useState} from 'react'; import ProductCustomizer from './ProductCustomizer'; import {useLanguage} from './LanguageProvider';
export default function BurgerCard({item}:{item:MenuItem}){const [open,setOpen]=useState(false);const {itemName,itemDescription,category,t}=useLanguage();const name=itemName(item);return <><article className="food-card" onClick={()=>setOpen(true)}><div className="food-visual"><Image src={item.image} alt={name} fill sizes="(max-width: 768px) 90vw, 33vw"/></div><div className="food-meta"><div><small>{category(item.category)}</small><h3>{name}</h3><p>{itemDescription(item)}</p></div><div className="food-bottom"><b>{t('card.from')} {item.price}</b><button aria-label={`Customize ${name}`} onClick={e=>{e.stopPropagation();setOpen(true)}}><Plus/></button></div></div></article>{open?<ProductCustomizer item={item} onClose={()=>setOpen(false)}/>:null}</>}
