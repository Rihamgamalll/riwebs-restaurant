'use client';
import PageHero from '@/components/PageHero';import BurgerCard from '@/components/BurgerCard';import {menuItems} from '@/data/menu';import Image from 'next/image';import {useLanguage} from '@/components/LanguageProvider';
export default function Burgers(){const b=menuItems.filter(x=>x.category==='Burgers');const {lang}=useLanguage();return <main><PageHero kicker="page.burgers.kicker" title="page.burgers.title" copy="page.burgers.copy"/><section className="section"><div className="food-grid">{b.map(x=><BurgerCard key={x.id} item={x}/>)}</div></section><section className="detail-strip section"><div><span className="section-kicker">{lang==='ar'?'داخل البرجر المميز':'INSIDE THE SIGNATURE'}</span><h2>{lang==='ar'?<>قرمشة.<br/>جبنة سايحة.<br/>طازة.</>:<>Crunch.<br/>Melt.<br/>Freshness.</>}</h2></div><div className="detail-burger"><Image src="/assets/full-burger.png" alt="RiWebs signature burger" fill sizes="560px"/></div><div className="detail-notes"><p><b>01</b> {lang==='ar'?'خبز سمسم محمص':'Toasted sesame brioche'}</p><p><b>02</b> {lang==='ar'?'خس وطماطم طازة':'Fresh lettuce + tomato'}</p><p><b>03</b> {lang==='ar'?'صوص جبنة كريمي':'Creamy cheese sauce'}</p><p><b>04</b> {lang==='ar'?'تشيكن كريسبي ذهبي':'Golden crispy chicken'}</p></div></section></main>}
